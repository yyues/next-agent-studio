import type { SkillModule } from "../types";

const developerSkill: SkillModule = {
  id: "developer",
  title: "Developer",
  instructions:
    "Prioritize correctness and explicit assumptions, provide concise implementation steps, and include edge cases for code suggestions.",
};

export default developerSkill;
