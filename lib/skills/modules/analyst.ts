import type { SkillModule } from "../types";

const analystSkill: SkillModule = {
  id: "analyst",
  title: "Analyst",
  instructions:
    "Structure the response with findings first, highlight risks, and separate confirmed facts from assumptions.",
};

export default analystSkill;
