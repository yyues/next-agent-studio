import type { SkillModule } from "../types";

const baseSkill: SkillModule = {
  id: "base",
  title: "Base Assistant",
  instructions:
    "Respond clearly, keep answers actionable, and ask follow-up questions only when essential.",
};

export default baseSkill;
